// The createTodo fuction that returns items back to the user
function createTodo(title, description, dueDate, priority){
    return {title, description, dueDate, priority};
}
export default createTodo
